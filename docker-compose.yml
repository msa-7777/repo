services:
  postgres:
    image: postgres:16
    container_name: logistics-postgres

    environment:
      POSTGRES_DB: ${POSTGRES_DB}
      POSTGRES_USER: ${POSTGRES_USER}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}

    ports:
      - "${POSTGRES_HOST_PORT}:5432"

    volumes:
      - postgres-data:/var/lib/postgresql/data
      - ./docker/postgres/init:/docker-entrypoint-initdb.d

    healthcheck:
      test:
        [
          "CMD-SHELL",
          "pg_isready -U ${POSTGRES_USER} -d ${POSTGRES_DB}"
        ]
      interval: 5s
      timeout: 5s
      retries: 10


#  eureka-server:
#    build:
#      context: .
#      dockerfile: eureka-server/Dockerfile
#    container_name: eureka-server
#    ports:
#      - "19090:19090"


#  company-service:
#    build:
#      context: .
#      dockerfile: company-service/Dockerfile
#
#    container_name: company-service
#
#    environment:
#      DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}?currentSchema=company_schema
#      DB_USERNAME: ${POSTGRES_USER}
#      DB_PASSWORD: ${POSTGRES_PASSWORD}
#      DB_SCHEMA: company_schema
#      EUREKA_URL: http://eureka-server:19090/eureka/
#
#    depends_on:
#      postgres:
#        condition: service_healthy


#  product-service:
#    build:
#      context: .
#      dockerfile: product-service/Dockerfile
#
#    container_name: product-service
#
#    environment:
#      DB_URL: jdbc:postgresql://postgres:5432/${POSTGRES_DB}?currentSchema=product_schema
#      DB_USERNAME: ${POSTGRES_USER}
#      DB_PASSWORD: ${POSTGRES_PASSWORD}
#      DB_SCHEMA: product_schema
#      EUREKA_URL: http://eureka-server:19090/eureka/
#
#      SPRING_JPA_HIBERNATE_DDL_AUTO: create
#
#    depends_on:
#      postgres:
#        condition: service_healthy
#
#volumes:
#  postgres-data: