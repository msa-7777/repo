server:
  port: 19099

spring:
  application:
    name: ai-service
  datasource:
    url: ${DB_URL:jdbc:postgresql://localhost:5432/postgres}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
  ai:
    openai:
      api-key: ${AI_API_KEY}
      base-url: https://generativelanguage.googleapis.com/v1beta/openai/
      chat:
        options:
          model: gemini-3.5-flash
#        embedding:
#          options:
#            model: text-embedding-004
#    model:
#      embedding: transformers

#  ai:
#    openai:
#      # 발급받으신 Gemini API 키
#      api-key: ${GEMINI_API_KEY}
#      # 끝에 반드시 슬래시(/)가 포함된 호환 URL
#      base-url: https://generativelanguage.googleapis.com/v1beta/openai/
#      chat:
#        options:
#          # Google AI Studio OpenAI 호환 호스트 지원 정식 모델명
#          model: gemini-1.5-flash
#          temperature: 0.2

# TODO : 임시 유레카 설정. 추후 수정 요망
## Eureka Client 설정 (포트 19090)
#eureka:
#  client:
#    service-url:
#      defaultZone: http://localhost:19090/eureka/
#  instance:
#    prefer-ip-address: true
#
## Hub 서비스 연동 포트 (19093)
#hub-service:
#  url: ${HUB_SERVICE_URL:http://localhost:19093}
## 슬랙 연동
slack:
  webhook-url: ${SLACK_WEBHOOK_URL:https://hooks.slack.com/services/test/test/test}

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URL:http://localhost:19090/eureka/}